# 📚 API 명세서

> **마지막 업데이트**: 2026-02-05  
> 마이데이터 테스트베드 프로젝트의 URL 엔드포인트 명세입니다.

---

## 목차

- [메인](#메인)
- [회원 (Member)](#회원-member)
- [고객지원 - 공지사항 (Notice)](#고객지원---공지사항-notice)
- [고객지원 - FAQ](#고객지원---faq)
- [고객지원 - 문의하기 (Inquiry)](#고객지원---문의하기-inquiry)
- [고객지원 - 자료실 (Resource)](#고객지원---자료실-resource)
- [고객지원 - 자유게시판 (Board)](#고객지원---자유게시판-board)
- [API가이드 - 데이터 표준 API](#api가이드---데이터-표준-api) ✅
- [API가이드 - 마이데이터 인증 API 규격](#api가이드---마이데이터-인증-api-규격) ✅
- [API가이드 - 마이데이터 지원 API 규격](#api가이드---마이데이터-지원-api-규격) ✅
- [API가이드 - 마이데이터 정보제공 API 규격 (은행)](#api가이드---마이데이터-정보제공-api-규격-은행) ✅ NEW
- [인증 필요 여부 요약](#인증-필요-여부-요약)

---

## 메인

### 메인 페이지

| 항목 | 내용 |
|------|------|
| **URL** | `/` |
| **Method** | `GET` |
| **인증** | 불필요 |
| **설명** | 메인 페이지 (최신 공지사항 3개 표시) |

**Model 데이터**:

| 속성 | 타입 | 설명 |
|------|------|------|
| `notices` | `List<NoticeListResponseDto>` | 최신 공지사항 3개 |

---

## 회원 (Member)

### 로그인

| 항목 | 내용 |
|------|------|
| **URL** | `/member/login` |
| **Method** | `GET` / `POST` |
| **인증** | 불필요 |
| **설명** | 로그인 페이지 및 처리 (Spring Security) |

**POST 파라미터** (Spring Security 자동 처리):

| 파라미터 | 타입 | 필수 | 설명 |
|----------|------|------|------|
| `email` | String | ✅ | 사용자 이메일 |
| `password` | String | ✅ | 비밀번호 |

**응답**:
- 성공: `/` (메인 페이지)
- 실패: `/member/login?error=true`

---

### 로그아웃

| 항목 | 내용 |
|------|------|
| **URL** | `/member/logout` |
| **Method** | `POST` |
| **인증** | 필요 |
| **설명** | 로그아웃 처리 (Spring Security) |

**응답**: `/` (메인 페이지)

---

### 회원가입 Step 1 - 약관동의

| 항목 | 내용 |
|------|------|
| **URL** | `/member/signup/step1` |
| **Method** | `GET` |
| **인증** | 불필요 |

---

### 회원가입 Step 2 - 휴대폰 인증

| 항목 | 내용 |
|------|------|
| **URL** | `/member/signup/step2` |
| **Method** | `GET` / `POST` |
| **인증** | 불필요 |

**POST 파라미터**:

| 파라미터 | 타입 | 필수 | 설명 |
|----------|------|------|------|
| `termsAgreed` | boolean | ✅ | 이용약관 동의 |
| `privacyAgreed` | boolean | ✅ | 개인정보처리방침 동의 |

---

### 회원가입 Step 3 - 회원정보 입력

| 항목 | 내용 |
|------|------|
| **URL** | `/member/signup/step3` |
| **Method** | `GET` / `POST` |
| **인증** | 불필요 |
| **설명** | 회원 생성 + 인증 메일 발송 |

**POST 파라미터** (`MemberSignupRequestDto`):

| 파라미터 | 타입 | 필수 | 검증 규칙 | 설명 |
|----------|------|------|-----------|------|
| `email` | String | ✅ | 이메일 형식, 중복 불가 | 사용자 이메일 |
| `password` | String | ✅ | 8~20자, 영문+숫자+특수문자 | 비밀번호 |
| `passwordConfirm` | String | ✅ | password와 일치 | 비밀번호 확인 |
| `name` | String | ✅ | 2~50자 | 이름 |
| `phone` | String | ✅ | 010-XXXX-XXXX 형식 | 휴대폰 번호 |
| `company` | String | ❌ | 최대 100자 | 소속 회사/기관 |
| `department` | String | ❌ | 최대 50자 | 부서 |

---

### 회원가입 Step 4 - 이메일 인증 대기

| 항목 | 내용 |
|------|------|
| **URL** | `/member/signup/step4` |
| **Method** | `GET` |
| **인증** | 불필요 |

---

### 이메일 인증 처리

| 항목 | 내용 |
|------|------|
| **URL** | `/member/verify-email` |
| **Method** | `GET` |
| **인증** | 불필요 |

**Query 파라미터**:

| 파라미터 | 타입 | 필수 | 설명 |
|----------|------|------|------|
| `token` | String | ✅ | 이메일 인증 토큰 (UUID) |

---

### 인증 메일 재발송

| 항목 | 내용 |
|------|------|
| **URL** | `/member/resend-verification` |
| **Method** | `POST` |
| **인증** | 불필요 |

---

## 고객지원 - 공지사항 (Notice)

### 공지사항 목록 조회

| 항목 | 내용 |
|------|------|
| **URL** | `/support/notice` |
| **Method** | `GET` |
| **인증** | 불필요 |

---

### 공지사항 상세 조회

| 항목 | 내용 |
|------|------|
| **URL** | `/support/notice/{id}` |
| **Method** | `GET` |
| **인증** | 불필요 |

---

## 고객지원 - FAQ

### FAQ 목록 조회

| 항목 | 내용 |
|------|------|
| **URL** | `/support/faq` |
| **Method** | `GET` |
| **인증** | 불필요 |
| **설명** | 카테고리별 필터링, 아코디언 UI |

---

## 고객지원 - 문의하기 (Inquiry)

### 문의 작성/등록

| 항목 | 내용 |
|------|------|
| **URL** | `/support/inquiry` |
| **Method** | `GET` / `POST` |
| **인증** | ✅ 필요 |

### 내 문의 목록

| 항목 | 내용 |
|------|------|
| **URL** | `/support/inquiry/list` |
| **Method** | `GET` |
| **인증** | ✅ 필요 |

### 문의 상세 조회

| 항목 | 내용 |
|------|------|
| **URL** | `/support/inquiry/{id}` |
| **Method** | `GET` |
| **인증** | ✅ 필요 |

---

## 고객지원 - 자료실 (Resource)

### 자료 목록/상세/다운로드

| URL | Method | 인증 | 설명 |
|-----|--------|------|------|
| `/support/resource` | GET | ❌ | 자료 목록 |
| `/support/resource/{id}` | GET | ❌ | 자료 상세 |
| `/support/resource/{id}/download` | GET | ❌ | 파일 다운로드 |

---

## 고객지원 - 자유게시판 (Board)

| URL | Method | 인증 | 설명 |
|-----|--------|------|------|
| `/support/board` | GET | ❌ | 게시글 목록 |
| `/support/board/{id}` | GET | ❌ | 게시글 상세 |
| `/support/board/{id}/download` | GET | ❌ | 첨부파일 다운로드 |
| `/support/board/write` | GET/POST | ✅ | 게시글 작성 |
| `/support/board/{id}/edit` | GET/POST | ✅ | 게시글 수정 (작성자/관리자) |
| `/support/board/{id}/delete` | POST | ✅ | 게시글 삭제 (작성자/관리자) |

---

## API가이드 - 데이터 표준 API ✅

> **Controller**: `ApiGuideController.java` (`@RequestMapping("/api-guide")`)  
> **사이드바**: `sidebar-api-spec.html` (아코디언), `activeGroup = "guide"`  
> **CSS**: `api-guide.css`, `sidebar.css`  
> **인증**: 모두 불필요

### 데이터 표준 API 기본규격

| 항목 | 내용 |
|------|------|
| **URL** | `/api-guide/base` |
| **Method** | `GET` |
| **인증** | 불필요 |
| **템플릿** | `api-guide/basic-spec.html` |
| **설명** | 데이터 표준 API 기본규격 (HTTP 메서드, URI, 헤더, 메시지 형식, 응답코드 등) |

### 데이터 표준 API 인증규격

| 항목 | 내용 |
|------|------|
| **URL** | `/api-guide/auth` |
| **Method** | `GET` |
| **인증** | 불필요 |
| **템플릿** | `api-guide/auth-spec.html` |
| **설명** | OAuth 2.0 인증규격, 접근토큰(JWS), 권한(scope), 유효기간 |

### 마이데이터 참여자별 API 처리 절차

| 항목 | 내용 |
|------|------|
| **URL** | `/api-guide/process` |
| **Method** | `GET` |
| **인증** | 불필요 |
| **템플릿** | `api-guide/process-spec.html` |
| **설명** | 사전준비, 전송요구·변경·철회(개별인증/통합인증), 정보전송 절차 |

### 리다이렉트

| 항목 | 내용 |
|------|------|
| **URL** | `/api-guide` |
| **Method** | `GET` |
| **설명** | `/api-guide/base`로 리다이렉트 |

---

## API가이드 - 마이데이터 인증 API 규격 ✅

> **Controller**: `CertApiController.java` (`@RequestMapping("/cert-api")`)  
> **사이드바**: `sidebar-api-spec.html` (아코디언), `activeGroup = "cert"`  
> **CSS**: `api-guide.css` (api-spec-card, api-version-badge, method-badge, api-msg-table 등)  
> **인증**: 모두 불필요

### 개별인증 API

| 항목 | 내용 |
|------|------|
| **URL** | `/cert-api/individual` |
| **Method** | `GET` |
| **인증** | 불필요 |
| **템플릿** | `cert-api/individual-api.html` (약 42KB) |
| **설명** | 개별인증 API 4개 스펙 (각 v0, v2 버전) |

**포함된 API 스펙:**

| API ID | API명 | 버전 |
|--------|-------|------|
| 개별인증-001 | 인가코드 발급 요청 | v0, v2 |
| 개별인증-002 | 접근토큰 발급 요청 | v0, v2 |
| 개별인증-003 | 접근토큰 갱신 | v0, v2 |
| 개별인증-004 | 접근토큰 폐기 | v0, v2 |

### 통합인증 API

| 항목 | 내용 |
|------|------|
| **URL** | `/cert-api/integrated` |
| **Method** | `GET` |
| **인증** | 불필요 |
| **템플릿** | `cert-api/integrated-api.html` (약 55KB) |
| **설명** | 통합인증 API 5개 스펙 (9개 버전) |

**포함된 API 스펙:**

| API ID | API명 | 버전 |
|--------|-------|------|
| 청소년 통합인증-002 | 청소년 통합인증 요청 | v2 |
| 통합인증-002 | 접근토큰 발급 요청(정보제공자 제공) | v0, v2 |
| 통합인증-101 | 접근토큰 발급 요청(통합인증기관 제공) | v0, v2 |
| 통합인증-102 | 전자서명 요청 | v0, v1 |
| 통합인증-103 | 전자서명 결과 조회 | v0, v1 |

### 리다이렉트

| 항목 | 내용 |
|------|------|
| **URL** | `/cert-api` |
| **Method** | `GET` |
| **설명** | `/cert-api/individual`로 리다이렉트 |

---

## API가이드 - 마이데이터 지원 API 규격 ✅

> **Controller**: `SupportApiController.java` (`@RequestMapping("/support-api")`)  
> **사이드바**: `sidebar-api-spec.html` (아코디언), `activeGroup = "support"`  
> **CSS**: `api-guide.css` (기존 스타일 재사용)  
> **인증**: 모두 불필요

### 지원 API(종합포털 제공)

| 항목 | 내용 |
|------|------|
| **URL** | `/support-api/portal` |
| **Method** | `GET` |
| **인증** | 불필요 |
| **템플릿** | `support-api/portal-api.html` |
| **설명** | 종합포털이 제공하는 지원 API 14개 스펙 |

**포함된 API 스펙:**

| API ID | API명 | 버전 | Method | URI |
|--------|-------|------|--------|-----|
| 지원-001 | 접근토큰 발급 (지원API 호출용) | v0 | POST | /mgmts/oauth/2.0/token |
| 지원-002 | 기관정보 조회 | v2 | GET | /v2/mgmts/orgs |
| 지원-002 | 기관정보 조회 | v1 | GET | /v1/mgmts/orgs |
| 지원-003 | 서비스정보 조회 | v2 | GET | /v2/mgmts/services |
| 지원-003 | 서비스정보 조회 | v1 | GET | /v1/mgmts/services |
| 지원-004 | 마이데이터사업자/정보수신자 통계자료 전송 | v1 | POST | /v1/mgmts/statistics/mydata |
| 지원-004 | 마이데이터사업자/정보수신자 통계자료 전송 | v2 | POST | /v2/mgmts/statistics/mydata |
| 지원-005 | 정보제공자 통계자료 전송 | v2 | POST | /v2/mgmts/statistics/provider |
| 지원-005 | 정보제공자 통계자료 전송 | v1 | POST | /v1/mgmts/statistics/provider |
| 지원-006 | 통합인증기관용 기관정보 조회 | v1 | GET | /v1/mgmts/orgs_for_ca |
| 지원-006 | 통합인증기관용 기관정보 조회 | v2 | GET | /v2/mgmts/orgs_for_ca |
| 지원-105 | 개인(신용)정보 제3자 제공동의 내역 요청 | v2 | POST | /v2/mgmts/agreements |
| 지원-106 | 개인(신용)정보 제3자 제공동의에 따른 제공 내역 요청 | v2 | POST | /v2/mgmts/agreements/detail |
| 지원-107 | 개인(신용)정보 제3자 제공 동의 내역 철회 요청 | v2 | POST | /v2/mgmts/agreements/revoke |

### 지원 API(마이데이터사업자/정보제공자 제공)

| 항목 | 내용 |
|------|------|
| **URL** | `/support-api/provider` |
| **Method** | `GET` |
| **인증** | 불필요 |
| **템플릿** | `support-api/provider-api.html` |
| **설명** | 마이데이터사업자/정보제공자가 제공하는 지원 API 4개 스펙 |

**포함된 API 스펙:**

| API ID | API명 | 버전 | Method | URI |
|--------|-------|------|--------|-----|
| 지원-101 | 접근토큰 발급 (마이데이터사업자 상태조회 API 호출용) | v0 | POST | /mgmts/oauth/2.0/token |
| 지원-102 | 마이데이터사업자 상태조회 | v2 | GET | /v2/mgmts/status |
| 지원-103 | 전송요구 내역 조회 (종합포털 제공) | v2 | POST | /v2/mgmts/consents |
| 지원-104 | 통계자료 재전송 요청 (종합포털 제공) | v2 | POST | /v2/mgmts/statistics/request |

### 리다이렉트

| 항목 | 내용 |
|------|------|
| **URL** | `/support-api` |
| **Method** | `GET` |
| **설명** | `/support-api/portal`로 리다이렉트 |

---

## API가이드 - 마이데이터 정보제공 API 규격 (은행) ✅ NEW

> **Controller**: `InfoApiController.java` (`@RequestMapping("/info-api")`)  
> **사이드바**: `sidebar-api-spec.html` (아코디언), `activeGroup = "info"`  
> **CSS**: `api-guide.css` (기존 스타일 재사용)  
> **인증**: 모두 불필요

### 은행 업권 정보제공 API 규격

| 항목 | 내용 |
|------|------|
| **URL** | `/info-api/bank` |
| **Method** | `GET` |
| **인증** | 불필요 |
| **템플릿** | `info-api/bank-api.html` (약 198KB) |
| **설명** | 은행 업권 정보제공 API 31개 스펙 (모두 v2) |

**포함된 API 스펙 (31개):**

| API ID | API명 | 버전 | Method | URI |
|--------|-------|------|--------|-----|
| DB-001 (은행) | DB형 퇴직연금정보 기본정보 조회 | v2 | GET | /v2/bank/db/basic |
| DC-001 (은행) | DC형 퇴직연금정보 목록정보 조회 | v2 | GET | /v2/bank/dc |
| DC-002 (은행) | DC형 퇴직연금정보 기본정보 조회 | v2 | POST | /v2/bank/dc/basic |
| DC-003 (은행) | DC형 퇴직연금정보 거래내역정보 조회 | v2 | POST | /v2/bank/dc/transactions |
| DC-004 (은행) | DC형 퇴직연금정보 추가정보 조회 | v2 | POST | /v2/bank/dc/detail |
| IRP-001 (은행) | 개인형 IRP 계좌 목록 조회 | v2 | GET | /v2/bank/irps |
| IRP-002 (은행) | 개인형 IRP 계좌 기본정보 조회 | v2 | POST | /v2/bank/irps/basic |
| IRP-003 (은행) | 개인형 IRP 계좌 추가정보 조회 | v2 | POST | /v2/bank/irps/detail |
| IRP-004 (은행) | 개인형 IRP 계좌 거래내역 조회 | v2 | POST | /v2/bank/irps/transactions |
| 선불-001_1 | 선불카드 목록 조회 | v2 | GET | /v2/bank/prepaid |
| 선불-002_1 | 선불카드 잔액정보 조회 | v2 | POST | /v2/bank/prepaid/balance |
| 선불-003_1 | 선불카드 거래내역 조회 | v2 | POST | /v2/bank/prepaid/transactions |
| 선불-004_1 | 선불카드 승인내역(결제내역) 조회 | v2 | POST | /v2/bank/prepaid/paid |
| 숨은금융-001 (은행) | 숨은 금융자산(미출연) 목록 조회 | v2 | GET | /v2/bank/hidden |
| 숨은금융-002 | 휴면예금 정보 조회 | v2 | GET | /v2/bank/dormant |
| 은행-001 | 계좌 목록 조회 | v2 | GET | /v2/bank/accounts |
| 은행-002 | 수신계좌 기본정보 조회 | v2 | POST | /v2/bank/accounts/deposit/basic |
| 은행-003 | 수신계좌 추가정보 조회 | v2 | POST | /v2/bank/accounts/deposit/detail |
| 은행-004 | 수신계좌 거래내역 조회 | v2 | POST | /v2/bank/accounts/deposit/transactions |
| 은행-005 | 펀드상품계좌 기본정보 조회 | v2 | POST | /v2/bank/accounts/invest/basic |
| 은행-006 | 펀드상품계좌 추가정보 조회 | v2 | POST | /v2/bank/accounts/invest/detail |
| 은행-007 | 펀드상품계좌 거래내역 조회 | v2 | POST | /v2/bank/accounts/invest/transactions |
| 은행-008 | 대출상품계좌 기본정보 조회 | v2 | POST | /v2/bank/accounts/loan/basic |
| 은행-009 | 대출상품계좌 추가정보 조회 | v2 | POST | /v2/bank/accounts/loan/detail |
| 은행-010 | 대출상품계좌 거래내역 조회 | v2 | POST | /v2/bank/accounts/loan/transactions |
| 은행-011 | 신탁/ISA 상품 기본 정보 조회 | v2 | POST | /v2/bank/accounts/isa/basic |
| 은행-012 | 신탁/ISA 상품 거래내역 정보 조회 | v2 | POST | /v2/bank/accounts/isa/transactions |
| 은행-013 | 신탁/ISA 개별운용상품 정보 조회 | v2 | POST | /v2/bank/accounts/isa/detail |
| 은행-014 | 계좌지정 자동이체 등록정보 | v2 | POST | /v2/bank/accounts/deposit/scheduled |
| 정보제공-공통-001 (은행) | API 목록 조회 | v0 | GET | /bank/apis |
| 정보제공-공통-002 (은행) | 전송요구 내역 조회 | v2 | GET | /v2/bank/consents |

### 리다이렉트

| 항목 | 내용 |
|------|------|
| **URL** | `/info-api` |
| **Method** | `GET` |
| **설명** | `/info-api/bank`로 리다이렉트 |

### 남은 업권 (추후 구현 예정)

| 업권 | URL | 상태 |
|------|-----|------|
| 카드 | `/info-api/card` | ⬜ |
| 보험 | `/info-api/insurance` | ⬜ |
| 금융투자 | `/info-api/invest` | ⬜ |
| 전자금융 | `/info-api/efin` | ⬜ |
| 할부금융 | `/info-api/installment` | ⬜ |
| 보증보험 | `/info-api/guarantee` | ⬜ |
| 통신 | `/info-api/telecom` | ⬜ |
| P2P | `/info-api/p2p` | ⬜ |
| 인수채권 | `/info-api/bond` | ⬜ |
| 대부 | `/info-api/loan` | ⬜ |
| 서민금융진흥원 | `/info-api/kinfa` | ⬜ |

---

## API 스펙 페이지 구성 패턴

각 API 스펙은 동일한 구조를 따름:
```
API 제목 | 버전 배지(v0/v1/v2) | 날짜
──────────────────────────────────
기본 정보 테이블:
  API ID, HTTP Method(배지), API 제공자, API 요청자, API명(URI), 설명, 기준시점, Content-Type

요청 메시지 명세 테이블:
  HTTP | 항목명 | 항목설명 | 필수 | 타입(길이) | 설명(비고)

응답 메시지 명세 테이블:
  HTTP | 항목명 | 항목설명 | 필수 | 타입(길이) | 설명(비고)
```

---

## 아코디언 사이드바 구조 (API 규격 섹션 공통)

> **템플릿**: `layout/sidebar-api-spec.html`  
> **파라미터**: `activeGroup` (String), `currentMenu` (String)

모든 API 규격 페이지에서 동일한 아코디언 사이드바를 공유합니다.  
`activeGroup`에 따라 해당 그룹이 펼쳐지고, 나머지는 접힌 상태입니다.

```
┌─────────────────────────────────┐
│ API가이드              (guide)   │ ← activeGroup='guide'이면 펼침
│   ├ 데이터 표준 API 기본규격     │
│   ├ 데이터 표준 API 인증규격     │
│   └ 마이데이터 참여자별 API 처리 │
├─────────────────────────────────┤
│ 마이데이터 인증 API 규격 (cert)  │ ← activeGroup='cert'이면 펼침
│   ├ 개별인증 API                 │
│   └ 통합인증 API                 │
├─────────────────────────────────┤
│ 마이데이터 지원 API 규격(support)│ ← activeGroup='support'이면 펼침 ✅
│   ├ 지원 API(종합포털 제공)       │
│   └ 지원 API(사업자/정보제공자)   │
├─────────────────────────────────┤
│ 마이데이터 정보제공 API규격(info)│ ← activeGroup='info'이면 펼침 ✅
│   ├ 은행 업권 정보제공 API 규격   │ ✅ (31개 API)
│   ├ 카드 업권 정보제공 API 규격   │ ⬜
│   ├ 보험 업권 정보제공 API 규격   │ ⬜
│   ├ 금융투자 업권 정보제공 API 규격│ ⬜
│   ├ 전자금융 업권 정보제공 API 규격│ ⬜
│   ├ 할부금융 업권 정보제공 API 규격│ ⬜
│   ├ 보증보험 업권 정보제공 API 규격│ ⬜
│   ├ 통신 업권 정보제공 API 규격   │ ⬜
│   ├ P2P 업권 정보제공 API 규격    │ ⬜
│   ├ 인수채권 업권 정보제공 API 규격│ ⬜
│   ├ 대부 업권 정보제공 API 규격   │ ⬜
│   └ 서민금융진흥원 API 규격       │ ⬜
└─────────────────────────────────┘
```

---

## 인증 필요 여부 요약

### 공개 URL (인증 불필요)

| URL 패턴 | 설명 |
|----------|------|
| `/css/**`, `/js/**`, `/images/**` | 정적 리소스 |
| `/` | 메인 페이지 |
| `/intro/**`, `/api-guide/**` | 소개, API 가이드 |
| `/cert-api/**` | 마이데이터 인증 API 규격 |
| `/support-api/**` | 마이데이터 지원 API 규격 ✅ |
| `/info-api/**` | 마이데이터 정보제공 API 규격 ✅ NEW |
| `/member/login` | 로그인 |
| `/member/signup/**` | 회원가입 |
| `/member/verify-email` | 이메일 인증 |
| `/member/resend-verification` | 인증 메일 재발송 |
| `/support/notice/**` | 공지사항 |
| `/support/faq` | FAQ |
| `/support/resource/**` | 자료실 |
| `/support/board` | 자유게시판 목록 |
| `/support/board/{id}` | 자유게시판 상세 |
| `/support/board/{id}/download` | 자유게시판 첨부파일 다운로드 |
| `/h2-console/**` | H2 콘솔 (개발용) |

### 인증 필요 URL

| URL 패턴 | 설명 |
|----------|------|
| `/support/inquiry/**` | 문의하기 (작성, 목록, 상세) |
| `/support/board/write` | 게시글 작성 |
| `/support/board/*/edit` | 게시글 수정 (작성자/관리자) |
| `/support/board/*/delete` | 게시글 삭제 (작성자/관리자) |
| `/testbed/**` | 테스트베드 (예정) |
| `/conformance/**` | 적합성 심사 (예정) |
| `/admin/**` | 관리자 (ADMIN 권한, 예정) |

---

## SecurityConfig 참고

```java
http.authorizeHttpRequests(auth -> auth
    // 정적 리소스
    .requestMatchers("/css/**", "/js/**", "/images/**").permitAll()
    
    // 공개 페이지
    .requestMatchers("/", "/intro/**", "/api-guide/**").permitAll()
    .requestMatchers("/cert-api/**").permitAll()
    .requestMatchers("/support-api/**").permitAll()   // ✅ 지원 API 규격
    .requestMatchers("/info-api/**").permitAll()       // ✅ 정보제공 API 규격
    .requestMatchers("/member/login", "/member/signup/**").permitAll()
    .requestMatchers("/member/verify-email", "/member/resend-verification").permitAll()
    
    // 고객지원 - 인증 필요 (순서 중요! 구체적인 것 먼저)
    .requestMatchers("/support/inquiry/**").authenticated()
    .requestMatchers("/support/board/write").authenticated()
    .requestMatchers("/support/board/*/edit").authenticated()
    .requestMatchers("/support/board/*/delete").authenticated()
    
    // 고객지원 - 공개
    .requestMatchers("/support/**").permitAll()
    
    // H2 콘솔 (개발용)
    .requestMatchers("/h2-console/**").permitAll()
    
    // 나머지는 인증 필요
    .anyRequest().authenticated()
);
```
